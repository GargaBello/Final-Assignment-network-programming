//snapshot.cpp

#include "snapshot.hpp"

namespace meteor {
	
	///*snapshot::snapshot(uint32 tick, std::vector<player> players, std::vector<terrain> terrain, std::vector<bomb> bombs)
	//	: m_tick(tick)
	//	, m_players(players)
	//	, m_terrain(terrain)
	//	, m_bombs(bombs)
	//{
	//}*/


	//void snapshot_queue::create_snapshot(uint32 tick)
	//{
	//	/*snapshot shot(tick, m_all_players, m_all_terrain, m_all_bombs);

	//	m_snapshots.emplace(m_snapshots.begin(), shot);*/

	//	/*if (m_snapshots.size() > MAX_SNAPSHOTS) {
	//		m_snapshots.pop_back();
	//	}*/
	//}

	///*void create_snapshot(uint32 sequence, background background)
	//{
	//	snapshot shot(sequence, m_all_players, m_all_terrain, m_all_bombs, background);

	//	
	//}*/
}